.DS_Store
__pycache__/
*.pyc
# the ring's live runtime files (see state/README.md)
state/state
state/mood.json
state/wave.json
# your personal config, created at setup: never tracked, never touched by updates
barehands.json

# Android build output and local SDK used to build the APK
.android-sdk/
.gradle/
build/
app/build/
*.bak
